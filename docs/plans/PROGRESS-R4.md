# Round 4 重构执行进度

## Phase A: P0 Bug 修复
- [x] Task A.1: 修复 `channels.StartAll` 假成功与 ready 误报；完成时间：2026-03-08 04:23 CST；改动摘要：为全量启动失败补回归测试，并在 `StartAll` 中统计成功启动数，0 成功时返回错误且保持 not ready；验证命令：`go test ./pkg/channels -run 'StartAll|Ready' -count=1`；结果：通过
- [x] Task A.2: 修复 `StopAll` 后发送 panic 风险（完成时间：2026-03-08 04:27:28 CST；改动摘要：`StopAll` 先取消 dispatcher 并摘除 `workers` 映射，再等待 worker 退出而不关闭队列，`SendToChannel` 在无 active worker 时统一返回 `ErrNotRunning`，并补充停止后 direct send / dispatcher 不再投递的回归测试；验证命令：`go test ./pkg/channels -run 'StopAll|SendToChannel|Dispatcher' -count=1`；结果：通过）
- [x] Task A.3: 让 Gateway reload 具备原子切换 / 回滚语义，并修复 startup 误报；完成时间：2026-03-08 04:41 CST；改动摘要：`reload` 改为先准备新 runtime 与 HTTP 注册、再停旧启新，若新 manager 启动失败则自动回滚恢复旧 manager / 旧 config；同时让 `setupCronTool` 返回错误而非 `log.Fatalf`，并在 `runGateway` 中仅在 cron / heartbeat 真正启动成功后打印成功日志；附带修复 `channels.StartAll` 里共享 HTTP server goroutine 闭包读空指针竞态；验证命令：`go test ./cmd/x-claw/internal/gateway -run 'Reload|reload' -count=1`、`go test ./cmd/x-claw/internal/gateway -count=1`；结果：通过
- [x] Task A.4: 修复 provider factory 默认 base 逻辑与错误提示（完成时间：2026-03-08 04:23:37 CST；改动摘要：补充本地默认 `api_base` 场景回归测试，按最终生效 `api_base` 判断 `litellm`/`ollama`/`vllm` 是否必须 `api_key`，并把 OAuth 凭证缺失/过期提示统一改为本地 auth store / `api_key` 可行指引；验证命令：`go test ./pkg/providers -run 'Factory|DefaultAPIBase|Credential' -count=1`；结果：通过）

## Phase B: 稳定性与可观测性
- [x] Task B.1: 修复 `ContextBuilder` 并发安全问题；完成时间：2026-03-08 04:26 CST；改动摘要：为 `ContextBuilder` 的共享运行时字段增加 `runtimeMu` 快照读取/更新，并补充并发 setter/reader 的 race 回归测试；验证命令：`go test -race ./pkg/agent -run TestContextBuilderConcurrentSettersAndReaders -count=1`（修复前失败、修复后通过），`go test ./pkg/agent -run 'TestConcurrentBuildSystemPromptWithCache|TestContextBuilderConcurrentSettersAndReaders|TestBuildMessagesForSession|TestPruneHistoryForContext' -count=1`（通过）；结果：通过。环境说明：`go test -race ./pkg/agent -run 'Context|BuildMessages|SystemPrompt' -count=1` 在当前环境两次均 `exit -1` 且无输出，因此改用更小批次 race + 功能回归验证。
- [x] Task B.2: 为 `memoryScopes` 增加边界控制；完成时间：2026-03-08 04:32 CST；改动摘要：为 `ContextBuilder.memoryScopes` 增加基于最大条目数的 LRU 式回收，超限时淘汰最久未访问的 scoped `MemoryStore`，再次访问时可按磁盘内容重建且行为不变；验证命令：`go test ./pkg/agent -run 'TestMemoryForSession_BoundsScopedCache|TestMemoryForSession_RebuildsEvictedScopeWithoutBehaviorChange' -count=1`（修复前编译失败，修复后通过），`go test ./pkg/agent -run 'MemoryForSession|Context' -count=1`（通过），`gofmt -w pkg/agent/context.go pkg/agent/context_test.go && go test ./pkg/agent -run 'MemoryForSession|Context' -count=1`（通过）；结果：通过。
- [x] Task B.3: 为后台 shell session 表增加回收策略；完成时间：2026-03-08 04:36 CST；改动摘要：新增 `pkg/tools/shell_session_test.go`，覆盖 `completed` / `failed` / `killed` / `clear` / `remove` / 容量上限 六类行为，确认当前 `ProcessManager` 已具备终态会话容量回收与 `clear` / `remove` 语义，因此无需修改生产代码；验证命令：`go test ./pkg/tools -run 'ShellSession|BackgroundSession|SessionTable' -count=1`（通过），`gofmt -w pkg/tools/shell_session_test.go && go test ./pkg/tools -run 'ShellSession|BackgroundSession|SessionTable' -count=1`（通过）；结果：通过。
- [x] Task B.4: 为 `ToolRegistry` 增加 nil `ToolResult` 防御，并让旁路通知更可观测；完成时间：2026-03-08 04:36 CST；改动摘要：核对确认当前工作树已实现 `ToolRegistry.ExecuteWithContext(...)` 对 `nil ToolResult` 的标准错误结果折叠，并在 `pkg/agent/pipeline_notify.go` 中对旁路 `message` tool 的 `nil` / error 结果记录日志而非静默吞掉，本次无需追加代码改动；验证命令：`go test ./pkg/tools -run 'Registry|ExecuteWithContext' -count=1`（通过）；结果：通过。
- [x] Task B.5: 补足 trace / heartbeat / stream follow 的可观测性与健壮性；完成时间：2026-03-08 05:08 CST；改动摘要：为 `console_stream` 增加 follow 指针在 `tail` 后推进到 EOF 的修复，并引入 `consoleStreamTailLines` 注入点，使 rotate / truncate / 首次 tail 失败 / 客户端取消 都能被稳定回归；同时核对确认当前工作树已在 `pkg/heartbeat/service.go` 记录 `PublishOutbound` 失败日志，并在 `pkg/agent/loop_trace.go` 记录 `Sync` 失败日志；验证命令：`go test ./pkg/httpapi -run TestConsoleHandler_StreamFollowsRotate -count=1`（先失败后通过），`go test ./pkg/httpapi -run TestConsoleHandler_StreamInitialTailReadFailureIsObservable -count=1`（注入点修复后通过），`go test ./pkg/httpapi -run TestConsoleHandler_StreamFollowsTruncate -count=1`、`go test ./pkg/httpapi -run TestConsoleHandler_StreamStopsOnClientCancel -count=1`、`go test ./pkg/httpapi -run TestConsoleHandler_Tail -count=1`（均通过），`go test ./pkg/heartbeat -run 'Heartbeat' -count=1`（通过），`go test ./pkg/agent -run 'Trace' -count=1`（通过，编译校验）；结果：通过。环境说明：更宽的 `pkg/httpapi` / 多包聚合正则在当前环境多次 `exit -1` 或 `SIGKILL(137)`，因此改用单测级/分包验证。

## Phase C: 边界收敛与职责瘦身
- [x] Task C.1: 抽取共享 runtime bootstrap，并继续拆 Gateway 组合根；完成时间：2026-03-08 04:58 CST；改动摘要：抽出 `cmd/x-claw/internal/cliutil/runtime.go` 作为共享 runtime bootstrap，统一 `LoadConfig` / `CreateProvider` / model alias 回写 / `NewMessageBus` / `NewAgentLoop` 装配；Gateway 继续拆分为 `bootstrap.go` / `runtime.go` 等职责单元，入口层仅保留参数与生命周期控制；验证命令：`go test ./cmd/x-claw/internal/agent ./cmd/x-claw/internal/gateway -count=1`、`go test ./cmd/x-claw/internal/gateway -run 'Reload|reload' -count=1`；结果：通过。
- [x] Task C.2: 收敛 provider 选择边界，冻结或退役遗留选择路径；完成时间：2026-03-08 05:01 CST；改动摘要：明确生产运行时以 `pkg/providers/factory.go` / `CreateProvider` → `pkg/providers/factory_provider.go` / `CreateProviderFromConfig` 为单一事实来源，`pkg/providers/legacy_provider.go` 仅保留配置兼容包装；同时将本地默认 `api_base` 语义与 `pkg/agent/loop_fallback.go` 的 provider 选择路径对齐，避免双轨漂移；验证命令：`go test ./pkg/providers ./pkg/agent -run 'Factory|Fallback|Provider' -count=1`；结果：通过。
- [x] Task C.3: 继续拆 `ContextBuilder` / `AgentInstance` / `filesystem` 并退役 `toolloop.go`；完成时间：2026-03-08 04:58 CST；改动摘要：将 `ContextBuilder` 的 memory/runtime 辅助逻辑下沉到 `pkg/agent/context_memory.go`，将 `AgentInstance` 配置解析拆到 `pkg/agent/instance_config.go`，将文件系统底层 helper 拆到 `pkg/tools/filesystem_helpers.go`，并移除无生产引用的 `pkg/tools/toolloop.go`，仅保留回归测试覆盖；验证命令：`go test ./pkg/agent -run 'Context|Instance' -count=1`、`go test ./pkg/tools -run 'Filesystem|Toolloop' -count=1`；结果：通过。
- [x] Task C.4: 收敛 HTTPAPI 鉴权与文件 helper；完成时间：2026-03-08 05:08 CST；改动摘要：将 console API/UI/notify 相关鉴权收口到共享 `authorizeAPIKeyOrLoopback`，并将 `resolveConsolePath`、`tailLines` 与 stream 文件读取 helper 集中到 `pkg/httpapi/console_fs.go` / `console_stream.go`；同时修复 `tail` 后 follow 重复旧行与初始 tail 失败注入点失效两个真实问题，确认文件 helper 收口后的行为稳定；验证命令：`go test ./pkg/httpapi -run TestConsoleHandler_APIKeyRequiredWhenConfigured -count=1`、`go test ./pkg/httpapi -run TestNotifyHandler_APIKeyRequiredWhenConfigured -count=1`、`go test ./pkg/httpapi -run TestSessionModelHandler_SetGetClear -count=1`、`go test ./pkg/httpapi -run TestConsoleHandler_FileDownloadAllowedPrefixes -count=1`、`go test ./pkg/httpapi -run TestConsoleHandler_Tail -count=1`、`go test ./pkg/httpapi -run TestConsoleHandler_StreamFollowsRotate -count=1`、`go test ./pkg/httpapi -run TestConsoleHandler_StreamFollowsTruncate -count=1`、`go test ./pkg/httpapi -run TestConsoleHandler_StreamInitialTailReadFailureIsObservable -count=1`、`go test ./pkg/httpapi -run TestConsoleHandler_StreamStopsOnClientCancel -count=1`；结果：通过。环境说明：更宽的 `pkg/httpapi` 聚合正则在当前环境多次 `exit -1` 或 `SIGKILL(137)`，因此改用单测级/窄正则验证。

## 阶段验证
- [x] Gate A：`pkg/channels` / `cmd/x-claw/internal/gateway` / `pkg/providers` 定向测试通过；完成时间：2026-03-08 04:43 CST；验证命令：`go test ./pkg/channels ./cmd/x-claw/internal/gateway ./pkg/providers -count=1`、`GOMODCACHE=$PWD/.cache/gomod GOPATH=$PWD/.cache/gopath GOCACHE=$PWD/.cache/gocache go build -p 1 ./...`、`GOMODCACHE=$PWD/.cache/gomod GOPATH=$PWD/.cache/gopath GOCACHE=$PWD/.cache/gocache go vet ./...`；结果：通过
- [x] Gate B：`pkg/agent` race + `pkg/tools` / `pkg/httpapi` / `pkg/heartbeat` 定向测试通过；完成时间：2026-03-08 05:08 CST；验证摘要：`Task B.1` 已用 `go test -race ./pkg/agent -run TestContextBuilderConcurrentSettersAndReaders -count=1` 证明缺口并在修复后通过；`go test ./pkg/tools -run 'ShellSession|BackgroundSession|Registry|ExecuteWithContext' -count=1`、`go test ./pkg/httpapi -run TestConsoleHandler_Tail -count=1`、`go test ./pkg/httpapi -run TestConsoleHandler_StreamFollowsRotate -count=1`、`go test ./pkg/httpapi -run TestConsoleHandler_StreamFollowsTruncate -count=1`、`go test ./pkg/httpapi -run TestConsoleHandler_StreamInitialTailReadFailureIsObservable -count=1`、`go test ./pkg/httpapi -run TestConsoleHandler_StreamStopsOnClientCancel -count=1`、`go test ./pkg/heartbeat -run 'Heartbeat' -count=1` 均通过；结果：通过。环境说明：更大范围的 `pkg/agent` race、`pkg/httpapi` 聚合正则与合并包测试在主控环境多次被 `SIGKILL(137)` 或 `exit -1` 打断，因此采用任务内单项 race + 单测级/分包定向测试作为阶段验证。
- [x] Gate C：`cmd/x-claw` / `pkg/providers` / `pkg/agent` / `pkg/tools` / `pkg/httpapi` 定向测试通过；完成时间：2026-03-08 05:08 CST；验证摘要：`go test ./cmd/x-claw/... -count=1`、`go test ./pkg/providers ./pkg/agent -run 'Factory|Fallback|Provider' -count=1`、`go test ./pkg/agent -run 'Context|Instance' -count=1`、`go test ./pkg/tools -run 'Filesystem|Toolloop' -count=1`、`go test ./pkg/httpapi -run TestConsoleHandler_APIKeyRequiredWhenConfigured -count=1`、`go test ./pkg/httpapi -run TestNotifyHandler_APIKeyRequiredWhenConfigured -count=1`、`go test ./pkg/httpapi -run TestSessionModelHandler_SetGetClear -count=1`、`go test ./pkg/httpapi -run TestConsoleHandler_FileDownloadAllowedPrefixes -count=1`、`go test ./pkg/httpapi -run TestConsoleHandler_Tail -count=1`、`go test ./pkg/httpapi -run TestConsoleHandler_StreamFollowsRotate -count=1`、`go test ./pkg/httpapi -run TestConsoleHandler_StreamFollowsTruncate -count=1`、`go test ./pkg/httpapi -run TestConsoleHandler_StreamInitialTailReadFailureIsObservable -count=1`、`go test ./pkg/httpapi -run TestConsoleHandler_StreamStopsOnClientCancel -count=1` 均通过；结果：通过。环境说明：更宽的 `pkg/httpapi` 聚合正则在当前环境多次 `exit -1` 或 `SIGKILL(137)`，因此改用单测级/窄正则验证。

## 最终验证
- [x] `go build -p 1 ./...` 通过；完成时间：2026-03-08 05:08 CST；验证命令：`mkdir -p .cache/gomod .cache/gopath .cache/gocache && GOMODCACHE=$PWD/.cache/gomod GOPATH=$PWD/.cache/gopath GOCACHE=$PWD/.cache/gocache go build -p 1 ./...`；结果：通过。
- [x] `go vet ./...` 通过；完成时间：2026-03-08 05:08 CST；验证命令：`mkdir -p .cache/gomod .cache/gopath .cache/gocache && GOMODCACHE=$PWD/.cache/gomod GOPATH=$PWD/.cache/gopath GOCACHE=$PWD/.cache/gocache go vet ./...`；结果：通过。
- [x] `go test ./... -run '^$' -count=1` 通过；完成时间：2026-03-08 05:08 CST；验证命令：`mkdir -p .cache/gomod .cache/gopath .cache/gocache && GOMODCACHE=$PWD/.cache/gomod GOPATH=$PWD/.cache/gopath GOCACHE=$PWD/.cache/gocache go test ./... -run '^$' -count=1`；结果：通过。
- [x] 如环境允许，关键改动包的定向 `go test` / `go test -race` 已记录；完成时间：2026-03-08 05:08 CST；记录摘要：已在各 Task / Gate 条目中记录 `pkg/channels`、`cmd/x-claw/internal/gateway`、`pkg/providers`、`pkg/agent`、`pkg/tools`、`pkg/httpapi`、`pkg/heartbeat` 的定向验证，以及 `Task B.1` 的单项 `-race` 验证；环境说明：更宽的 `pkg/agent -race` 与部分 `pkg/httpapi` 聚合测试在当前环境会出现 `SIGKILL(137)` 或 `exit -1`，因此已改用更小批次命令并逐项写明。

## 记录要求
- 每完成一个任务，将 `[ ]` 改为 `[x]`
- 在对应条目后追加：完成时间、改动摘要、验证命令、结果
- 若遇到环境限制（如 `SIGKILL(137)`），必须写明“实际执行了什么”和“为什么改用更小批次验证”
